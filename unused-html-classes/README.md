# HTML Class Checker

Extension that checks which CSS classes and IDs used in an HTML file are defined in local CSS files or external links, and which ones are unused.

## Installation

### From VSIX file
1. Open VS Code
2. Press `Ctrl + Shift + X` to open the Extensions panel
3. Click the three dots `···` in the top right corner of the panel
4. Click `Install from VSIX...`
5. Select the `unused-html-classes-0.0.1.vsix` file
6. Restart VS Code when prompted

## How to use

1. Open an HTML file
2. Open the command palette with `Ctrl + Shift + P`
3. Search for `Check Unused HTML Classes` and press Enter
4. Check the "HTML Class Checker" panel for the results